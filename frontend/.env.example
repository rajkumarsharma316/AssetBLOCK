# API URL - set this to your deployed backend URL in Vercel dashboard
# For local development, leave empty (uses Vite proxy to localhost:3001)
# For production, set to your backend URL (e.g., https://your-backend.render.com/api)
VITE_API_URL=
